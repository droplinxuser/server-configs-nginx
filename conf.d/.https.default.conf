# ----------------------------------------------------------------------
# | Default behavior for unknown hosts                                 |
# ----------------------------------------------------------------------
#
# Drop requests for unknown hosts.
#
# If no default server is defined, Nginx will use the first found server.
# To prevent host header attacks, or other potential problems when an unknown
# server name is used in a request, it's recommended to drop the request
# returning 444 "No Response".

# rename this file to 1.https.default.conf
# modify certificate to use hostname_ssl

server {
  listen [::]:443 ssl http2 default_server deferred backlog=16383 reuseport fastopen=256;
  listen      443 ssl http2 default_server deferred backlog=16383 reuseport fastopen=256;

  server_name _;
  include h5bp/limit_req/*_medium.conf;

  include h5bp/tls/ssl_engine.conf;
  include h5bp/hostname_ssl.conf;
  include h5bp/tls/policy_balanced.conf;

  return 444;
}
